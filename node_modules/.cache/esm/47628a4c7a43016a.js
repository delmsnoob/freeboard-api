let addComment,fetchComments;_7a4‍.x([["createComment",()=>createComment],["getComments",()=>getComments]]);_7a4‍.w("../models/commentModel",[["addComment",["addComment"],function(v){addComment=v}],["fetchComments",["fetchComments"],function(v){fetchComments=v}]]);

// add comment
       const createComment = (req, res) => {
  const data = req.body
  addComment(data, (err, results) => {
    if (err) {
      res.send(err)
    } else {
      res.json(results)
    }
  })
}

// fetch reply by post id
       const getComments = (req, res) => {
  fetchComments(req.params.id, (err, results) => {
    if (err) {
      res.send(err)
    } else {
      res.json(results)
    }
  })
}