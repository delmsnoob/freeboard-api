let db;_159‍.x([["addComment",()=>addComment],["fetchComments",()=>fetchComments]]);_159‍.w("../config/database",[["default",["db"],function(v){db=v}]]);

// Add reply
       const addComment = (data, result) => {
  db.query('INSERT INTO comments SET ?', [data], (err, results) => {
    if (err) {
      _159‍.g.console.log(err)
      result(err, null)
    } else {
      result(null, results)
    }
  })
}

// fetch reply by post id
       const fetchComments = (result) => {
  db.query('SELECT * from comments', (err, results) => {
    if (err) {
      _159‍.g.console.log(err)
      result(err, null)
    } else {
      result(null, results[0])
    }
  })
}
